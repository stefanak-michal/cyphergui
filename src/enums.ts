export enum EPage {
    Start = "start",
    Query = "query",
    Node = "node",
    Label = "label",
    Type = "type",
    Rel = "relationship",
}

export enum EPropertyType {
    String = "String",
    Integer = "Integer",
    Float = "Float",
    Boolean = "Boolean",
}
